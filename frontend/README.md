# Frontend

Vite + React chatbot widget. Runs on `http://localhost:5173` by default in dev mode.

The frontend calls the backend at `/api/generate-notes`. When running locally, run the backend on port 4000 and configure a proxy if desired.
